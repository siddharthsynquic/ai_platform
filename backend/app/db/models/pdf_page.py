"""PdfPage ORM model — one row per PDF page (text, OCR text, dims)."""

# TODO: implement with PostgreSQL persistence.
# Fields: id, pdf_document_id (FK), page_number, width, height,
#         raw_text, ocr_text, is_scanned, extracted_at

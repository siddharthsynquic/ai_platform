"""Comment ORM — threaded discussion on an observation."""

# TODO: implement.
# Fields:
#   id, observation_id (FK), author_user_id (FK), parent_comment_id (for threads),
#   body, mentioned_user_ids (JSONB array)

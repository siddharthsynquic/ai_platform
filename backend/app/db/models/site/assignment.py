"""Assignment ORM — observation assigned to team member / contractor with deadline."""

# TODO: implement.
# Fields:
#   id, observation_id (FK), assigned_to_user_id (FK), assigned_to_party (contractor name if external),
#   assigned_by_user_id, assigned_at, deadline_at, priority, notes,
#   escalated_at, escalated_reason

"""Report ORM — generated site reports (daily / weekly / snag / custom)."""

# TODO: implement.
# Fields:
#   id, project_id (FK), report_type, generated_by_user_id, generated_at,
#   filters (JSONB — date range, floor, trade, severity),
#   storage_key (rendered PDF/Excel), delivery_status, recipients (JSONB)

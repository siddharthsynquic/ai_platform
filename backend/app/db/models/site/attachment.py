"""Attachment ORM — photo / voice / doc file linked to an observation."""

# TODO: implement.
# Fields:
#   id, observation_id (FK), storage_key (StorageProvider key), mime_type,
#   size_bytes, width, height, duration_seconds (for audio),
#   is_before_photo, is_after_photo, transcript, ai_description

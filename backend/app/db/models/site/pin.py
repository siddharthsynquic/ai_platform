"""Pin ORM — spatial anchor on a floor plan PDF page."""

# TODO: implement.
# Fields:
#   id, observation_id (FK), floor_plan_pdf_id (FK → PdfDocument),
#   page_number, x_ratio (0-1 normalized), y_ratio (0-1 normalized),
#   rendered_at_scale

// TODO: wire once backend `/validation/*` endpoints exist.
// export const validationApi = {
//   run: (fileId: string) => apiClient.post(`/validation/${fileId}/run`).then(unwrap),
//   report: (fileId: string) => apiClient.get(`/validation/${fileId}/report`).then(unwrap),
//   flags: (fileId: string) => apiClient.get(`/validation/${fileId}/flags`).then(unwrap),
// };
export {};
